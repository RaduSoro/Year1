java House houseConfig.txt 10 //compile the java class with the name of the txt config file and number of hours you want the simulation by default will run for 7*24 hrs
java House cfg.txt // minimal needed will not run otherwise.(cfg being the txt file you read from)
Code runs perfectly implemented feed in tariff wich sells energy back to the grid if the house has more than 100 battery left after 1 timePasses() and if the
feed in tariff is bigger than the meter units price.
